<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FSM_TMARagIndicatorMobile__c</fullName>
    <description>Created as part of the SFS-6685 for mobile user page layout due to limitation on mobile to show images (which is what the original field shows)</description>
    <externalId>false</externalId>
    <formula>IF(
    FSM_PermitEndDate__c &lt; NOW(), 
    &quot;🟥&quot;, 
    IF(
        FSM_PermitEndDate__c &lt;= NOW() + 1, 
        &quot;🟨&quot;, 
        IF(
            FSM_PermitEndDate__c &gt; NOW() + 1, 
            &quot;🟩&quot;, 
            &quot;&quot;
        )
    )
)</formula>
    <label>TMA Rag Indicator</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
